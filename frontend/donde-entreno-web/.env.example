# URL base del backend (API Spring Boot)
NEXT_PUBLIC_API_URL=http://localhost:8080

# URL pública del frontend. Opcional en local (default: http://localhost:3000).
# En producción, setearla con la URL real para metadata, robots.txt y sitemap.
# NEXT_PUBLIC_SITE_URL=http://localhost:3000
